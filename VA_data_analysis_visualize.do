
graph hbar (count), over(age_group) title("Distribution of Deaths among Age Groups")
// graph bar (count), over(age_group, gap(0)) title("Distribution of Age Groups") xla(, angle(45))
graph bar (count), over(age_group, gap(0)) title("Distribution of Age Groups") ///
    xlabel(, angle(45))


graph pie, over(age_group) title("Age Group Distribution")

